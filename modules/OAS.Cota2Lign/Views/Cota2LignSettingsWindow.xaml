<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="OpenAsphalte.Modules.Cota2Lign.Views.Cota2LignSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ParamÃ¨tres - Cotation entre deux lignes"
        Height="500" Width="450"
        MinHeight="480" MinWidth="400"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0"
        ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/OAS.Core;component/Resources/OasStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0"
                   x:Name="TitleLabel"
                   Text="Paramètres de cotation"
                   Style="{StaticResource OasWindowTitle}"/>

        <!-- Champs de paramÃ¨tres -->
        <StackPanel Grid.Row="1" VerticalAlignment="Top">

            <!-- Interdistance -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           x:Name="InterdistanceLabel"
                           Text="Interdistance (m) :"
                           Style="{StaticResource OasFieldLabel}"
                           ToolTip="Distance entre chaque cotation (0 pour dÃ©sactiver)"/>
                <TextBox x:Name="InterdistanceTextBox"
                         Grid.Column="1"
                         Style="{StaticResource OasFieldTextBox}"
                         ToolTip="Distance entre chaque cotation (0 pour dÃ©sactiver)"/>
            </Grid>

            <!-- DÃ©calage -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           x:Name="OffsetLabel"
                           Text="DÃ©calage cotation (m) :"
                           Style="{StaticResource OasFieldLabel}"
                           ToolTip="Distance de dÃ©calage du texte de cotation"/>
                <TextBox x:Name="OffsetTextBox"
                         Grid.Column="1"
                         Style="{StaticResource OasFieldTextBox}"
                         ToolTip="Distance de dÃ©calage du texte de cotation"/>
            </Grid>

            <!-- Calque de destination -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           x:Name="LayerLabel"
                           Text="Calque de destination :"
                           Style="{StaticResource OasFieldLabel}"
                           ToolTip="Calque sur lequel crÃ©er les cotations (vide = calque courant)"/>
                <TextBox x:Name="LayerTextBox"
                         Grid.Column="1"
                         Style="{StaticResource OasFieldTextBox}"
                         ToolTip="Calque sur lequel crÃ©er les cotations (vide = calque courant)"/>
            </Grid>

            <!-- Coter aux sommets -->
            <CheckBox x:Name="VerticesCheckBox"
                      Content="Coter aux sommets de la polyligne"
                      Style="{StaticResource OasFieldCheckBox}"
                      Margin="0,8,0,8"
                      ToolTip="Ajouter une cotation Ã  chaque sommet de la polyligne"/>

            <!-- Inverser le cÃ´tÃ© -->
            <CheckBox x:Name="ReverseSideCheckBox"
                      Content="Inverser le cÃ´tÃ© de dÃ©calage"
                      Style="{StaticResource OasFieldCheckBox}"
                      Margin="0,0,0,16"
                      ToolTip="Place les cotations de l'autre cÃ´tÃ© de la polyligne"/>

            <!-- Accrochage OAS -->
            <CheckBox x:Name="UseOasSnapCheckBox"
                      Content="Utiliser l'accrochage OAS"
                      Style="{StaticResource OasFieldCheckBox}"
                      Margin="0,8,0,0"
                      FontWeight="SemiBold"
                      ToolTip="Active l'accrochage intelligent OAS (nÃ©cessite le module DynamicSnap). Modes configurables via OAS_SNAP_SETTINGS."/>

        </StackPanel>

        <!-- Boutons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="ResetButton"
                    Grid.Column="0"
                    Content="RÃ©initialiser"
                    Style="{StaticResource OasWarningButton}"
                    Click="ResetButton_Click"
                    ToolTip="RÃ©tablir les valeurs par dÃ©faut"/>

            <Button x:Name="CancelButton"
                    Grid.Column="2"
                    Content="Annuler"
                    Style="{StaticResource OasSecondaryButton}"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>

            <Button x:Name="ApplyButton"
                    Grid.Column="3"
                    Content="Appliquer"
                    Style="{StaticResource OasPrimaryButton}"
                    Click="ApplyButton_Click"/>
        </Grid>
    </Grid>
</Window>
